@extends('layouts.app')

@section('content')
    <div class="d-flex justify-content-between mb-3">
        <h3>Prisijungimo raktai: {{ $school->name }}</h3>
        <div>
            <a href="{{ route('schools.login-keys.import', $school) }}" class="btn btn-success">
                <i class="bi bi-upload"></i> Importuoti
            </a>
            <a href="{{ route('schools.login-keys.export-pdf', $school) }}" class="btn btn-info">
                <i class="bi bi-file-pdf"></i> PDF
            </a>
        </div>
    </div>

    @if(session('success'))
        <div class="alert alert-success alert-dismissible fade show">
            {{ session('success') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    @endif

    <!-- Generate keys section -->
    <div class="card mb-4">
        <div class="card-header bg-primary text-white">
            <i class="bi bi-key"></i> Sugeneruoti naujus raktus
        </div>
        <div class="card-body">
            <form method="POST" action="{{ route('schools.login-keys.generate', $school) }}" class="row g-2">
                @csrf
                <div class="col-md-3">
                    <label class="form-label">Kiekis</label>
                    <input type="number" name="count" class="form-control" min="1" max="1000" value="10" required>
                </div>
                <div class="col-md-3">
                    <label class="form-label">Tipas</label>
                    <select name="type" class="form-select" id="typeSelect" required>
                        <option value="">-- Pasirinkite --</option>
                        <option value="student">Mokiniai</option>
                        <option value="teacher">Mokytojai</option>
                    </select>
                </div>
                <div class="col-md-3" id="classDiv" style="display:none;">
                    <label class="form-label">Klasė</label>
                    <select name="class_id" class="form-select">
                        <option value="">-- Pasirinkite klasę --</option>
                        @foreach($school->classes as $class)
                            <option value="{{ $class->id }}">{{ $class->name }}</option>
                        @endforeach
                    </select>
                </div>
                <div class="col-md-3">
                    <label class="form-label">&nbsp;</label>
                    <button type="submit" class="btn btn-primary w-100">Sukurti</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Search section -->
    <div class="card mb-4">
        <div class="card-body">
            <form method="GET" action="{{ route('schools.login-keys.search', $school) }}" class="row g-2">
                <div class="col-md-9">
                    <input type="text" name="q" class="form-control" placeholder="Ieškoti pagal raktą, vardą, pavardę arba el. paštą..." 
                           value="{{ request('q') }}">
                </div>
                <div class="col-md-3">
                    <button type="submit" class="btn btn-info w-100">
                        <i class="bi bi-search"></i> Ieškoti
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Keys table -->
    @if($loginKeys->isEmpty())
        <div class="alert alert-info">Nėra prisijungimo raktų.</div>
    @else
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-light">
                    <tr>
                        <th>Raktas</th>
                        <th>Tipas</th>
                        <th>Vardas</th>
                        <th>El. paštas</th>
                        <th>Klasė</th>
                        <th>Būsena</th>
                        <th>Vartotojas</th>
                        <th>Veiksmai</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach($loginKeys as $key)
                        <tr>
                            <td>
                                <code class="bg-light p-2">{{ $key->key }}</code>
                            </td>
                            <td>
                                <span class="badge {{ $key->type === 'student' ? 'bg-success' : 'bg-info' }}">
                                    {{ $key->type === 'student' ? 'Mokinys' : 'Mokytojas' }}
                                </span>
                            </td>
                            <td>{{ $key->first_name }} {{ $key->last_name }}</td>
                            <td>{{ $key->email ?: '-' }}</td>
                            <td>{{ $key->class ? $key->class->name : '-' }}</td>
                            <td>
                                <span class="badge {{ $key->used ? 'bg-success' : 'bg-warning' }}">
                                    {{ $key->used ? 'Naudotas' : 'Nenaudotas' }}
                                </span>
                            </td>
                            <td>
                                @if($key->user)
                                    <a href="{{ route('users.edit', $key->user) }}" title="Peržiūrėti vartotoją">
                                        {{ $key->user->name }}
                                    </a>
                                @else
                                    -
                                @endif
                            </td>
                            <td>
                                @if(!$key->used)
                                    <form action="{{ route('schools.login-keys.regenerate', [$school, $key]) }}" method="POST" class="d-inline">
                                        @csrf
                                        <button type="submit" class="btn btn-sm btn-warning" title="Regeneruoti raktą">
                                            <i class="bi bi-arrow-repeat"></i>
                                        </button>
                                    </form>
                                    <button type="button" class="btn btn-sm btn-danger" data-bs-toggle="modal" 
                                            data-bs-target="#deleteModal" 
                                            data-action="{{ route('schools.login-keys.destroy', [$school, $key]) }}">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                @endif
                            </td>
                        </tr>
                    @endforeach
                </tbody>
            </table>
        </div>
        {{ $loginKeys->links() }}
    @endif
</div>

<script>
document.getElementById('typeSelect').addEventListener('change', function() {
    const classDiv = document.getElementById('classDiv');
    classDiv.style.display = this.value === 'student' ? 'block' : 'none';
});

// Delete Modal Handler
const deleteModal = document.getElementById('deleteModal');
if (deleteModal) {
    deleteModal.addEventListener('show.bs.modal', function (e) {
        const button = e.relatedTarget;
        const action = button.getAttribute('data-action');
        document.getElementById('deleteForm').action = action;
    });
}
</script>

<!-- Delete Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Patvirtinti šalinimą</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                Ar tikrai norite ištrinti šį prisijungimo raktą? Šio veiksmo negalima atšaukti.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Atšaukti</button>
                <form id="deleteForm" method="POST" class="d-inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit" class="btn btn-danger">Ištrinti</button>
                </form>
            </div>
        </div>
    </div>
</div>
@endsection
